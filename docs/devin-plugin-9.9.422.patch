diff --git a/CHANGELOG.md b/CHANGELOG.md
index d42e557..00e6b68 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,7 @@
 # Changelog · dao-proxy-pro
 
+v9.9.422 · 修复 Devin 走 cccc 等 Anthropic 兼容中继时只写缓存、从不命中：适配器在 `openai-compatible` 渠道同时发送 `x-api-key` 与 `Authorization: Bearer`，路由层不再删除 Anthropic 请求的 Bearer。官方 `type=anthropic` 仍只发 `x-api-key`。同一会话后续请求可出现 `cache_read`/`cached > 0`。运行时锚点升为 9.9.422，避免 Devin 复用旧插件进程。
+
 v9.9.421 · 修复 Codex 热路由任务无输出直接结束：反代不再把 reasoning-only 或完全空的上游流视为成功正文，也不会发送空 `response.completed`。仅在无正文、无工具调用时同渠道透明重试一次，仍为空则按用户配置的渠道优先级切换，所有候选均为空才返回明确 Responses 错误；正常请求零额外轮次。新增 `/codex-hot/v1/models`，返回 Codex `ModelsResponse {models:[...]}` 而不影响普通 `/v1/models` 的 OpenAI `{data:[...]}` 契约；模型元数据复用本机 Codex 原生目录，保留基础提示词、工具模式、上下文及截断能力，只同步热路由模型标识和推理强度。Claude/Devin 质量路由保持 `devin-native`，插件不追加搜索工具、不执行代理内部 continuation、不改写 Devin 工具契约。新增流式/非流式 reasoning-only 恢复、连续空响应明确失败、备用渠道切换、双模型目录契约回归。
 
 v9.9.420 · Codex 与 Devin 文件差异双视图精确同步：⑧捕获器的同一份前后快照现在同时注册为 Devin/VS Code 原生 SCM `Codex / Devin 文件变更`，由 Devin `DiffZone` 和⑧面板共享 `filePath + beforeHash + afterHash` 指纹；绿色新增、红色删除、原生 Diff 与⑧接受/回退不再各自生成内容。点击差异前会即时重读磁盘并刷新 `afterHash`，防抖窗口内也不会打开旧内容；编辑器保存、工作区新增/删除/重命名与磁盘监听事件按来源合并为一个文件项。SCM Quick Diff 的原始侧直接读取⑧捕获的 preimage，当前侧读取真实文件；Accept 推进同一基线，Reject 继续校验当前哈希和未保存编辑器，发现 Codex 后又被 Devin/用户改动时拒绝覆盖。日志确认 `codex-acp` 处于禁用状态，旧的重复绿色差异并非本插件 ACP 适配器产生；本版不通过二次写盘伪造 ACP 工具卡，只把真实文件差异同步给 Devin 原生 SCM/DiffZone，因此不会再产生模型重写版 Diff。新增 tracker 来源/指纹与 SCM 桥单元回归。
diff --git a/README.md b/README.md
index ac1ed0e..ba474f4 100644
--- a/README.md
+++ b/README.md
@@ -2,6 +2,15 @@
 
 > GitHub 稳定版说明文档。仓库包含插件源码、严格项目提示词替换功能、跨电脑迁移指南和可直接安装的清洁版 VSIX。
 
+## v9.9.422 更新
+
+### Anthropic 兼容中继缓存命中
+
+- 修复 cccc 等 `openai-compatible` + Anthropic `/v1/messages` 渠道只写缓存、从不读取的问题。
+- 这类渠道现在同时发送 `x-api-key` 和 `Authorization: Bearer`；官方 Anthropic 仍只发 `x-api-key`。
+- 路由层不再删掉 Anthropic 请求的 Bearer。同一 Devin 会话里连续两轮，第二轮应出现 `cached > 0`。
+- 安装本版 `dao-proxy-pro-9.9.422.vsix` 后 Reload Window，让运行时锚点升到 9.9.422。
+
 ## v9.9.421 更新
 
 ### Codex 热路由空响应与原生模型目录修复
diff --git a/docs/RELEASE_9.9.422.md b/docs/RELEASE_9.9.422.md
new file mode 100644
index 0000000..7a547a1
--- /dev/null
+++ b/docs/RELEASE_9.9.422.md
@@ -0,0 +1,22 @@
+# dao-proxy-pro 9.9.422
+
+## Anthropic 兼容中继缓存命中
+
+cccc 一类渠道协议是 Anthropic `/v1/messages`，但账号类型是 `openai-compatible`。旧版只发 `x-api-key`，路由层还会删掉 `Authorization`，上游能写 5 分钟缓存，却读不回来。
+
+本版：
+
+1. `openai-compatible` 的 Anthropic 适配器同时发送 `x-api-key` 与 `Authorization: Bearer`。
+2. 官方 `type=anthropic` 仍只发 `x-api-key`。
+3. 路由层不再删除 Anthropic 请求的 Bearer。
+4. 运行时锚点升为 `v9.9.422-dao-fa-zi-ran`，避免 Devin 复用旧插件进程。
+
+验证：同一会话连续两轮短续问，第二轮用量里 `cached > 0`。只有 `cacheWrite` 不算命中。
+
+## 安装
+
+```bash
+devin-desktop --install-extension dao-proxy-pro-9.9.422.vsix --force
+```
+
+然后 Reload Window。
diff --git a/package.json b/package.json
index 87448cf..4c57987 100644
--- a/package.json
+++ b/package.json
@@ -2,7 +2,7 @@
   "name": "dao-proxy-pro",
   "displayName": "道Agent Pro · 外接API · 反者道之动",
   "description": "在 dao-proxy-min 反代/提示词隔离之上，新增外接第三方 API：多 Key/多端点加权负载均衡 + 故障转移、按渠道/模型用量与成本可见、配置原子写+备份轮转、八模块（本源观照/渠道配置/模型路由/模型反代/内网穿透/协议中转站/自定义模型/Codex热路由）、只填 API Key 自动识别模型。模型反代把已接通模型暴露为 OpenAI Chat/Responses、Anthropic 与 Gemini 本地端点，协议中转站可将 Chat-only 模型转换后供 Codex、Claude Code 等客户端直调。完整版本历史见 CHANGELOG。",
-  "version": "9.9.421",
+  "version": "9.9.422",
   "publisher": "dao-agi",
   "license": "Apache-2.0",
   "engines": {
@@ -238,7 +238,7 @@
   },
   "scripts": {
     "test:codex-hot-route": "node test/codex-hot-route.test.js && node test/codex-hot-endpoint.test.js && node test/codex-change-tracker.test.js && node test/codex-scm-bridge.test.js",
-    "test": "node vendor/外接api/core/dao-test.js && node test/codex-hot-route.test.js && node test/codex-hot-endpoint.test.js && node test/codex-change-tracker.test.js && node test/codex-scm-bridge.test.js && node test/workspace-tool-strategy.test.js && node test/local-workspace-tools.test.js && node test/acp-workspace-message.test.js && node test/acp-stdio-proxy.test.js && node test/workspace-ignore-guard.test.js",
+    "test": "node vendor/外接api/core/dao-test.js && node test/codex-hot-route.test.js && node test/codex-hot-endpoint.test.js && node test/codex-change-tracker.test.js && node test/codex-scm-bridge.test.js && node test/workspace-tool-strategy.test.js && node test/local-workspace-tools.test.js && node test/acp-workspace-message.test.js && node test/acp-stdio-proxy.test.js && node test/workspace-ignore-guard.test.js && node test/anthropic-relay-auth.test.js",
     "test:quick": "node vendor/外接api/core/dao-test.js --quick",
     "test:e2e": "node vendor/外接api/core/dao-test.js --e2e",
     "test:protocol": "node vendor/外接api/core/dao-test.js --protocol",
diff --git a/test/anthropic-relay-auth.test.js b/test/anthropic-relay-auth.test.js
new file mode 100644
index 0000000..86eaf66
--- /dev/null
+++ b/test/anthropic-relay-auth.test.js
@@ -0,0 +1,46 @@
+"use strict";
+
+const assert = require("node:assert");
+const fs = require("node:fs");
+const path = require("node:path");
+const adapters = require("../vendor/外接api/core/adapters.js");
+
+const anthropic = adapters.adapterFor("anthropic");
+assert.ok(anthropic, "Anthropic adapter must be available");
+
+const relayHeaders = anthropic.buildRequestOpts(
+  { type: "openai-compatible", apiKey: "relay-secret" },
+  {},
+  new URL("https://relay.invalid/v1/messages"),
+).headers;
+
+assert.strictEqual(relayHeaders["x-api-key"], "relay-secret");
+assert.strictEqual(
+  relayHeaders.Authorization,
+  "Bearer relay-secret",
+  "Anthropic-compatible relays need bearer affinity as well as x-api-key auth",
+);
+
+const officialHeaders = anthropic.buildRequestOpts(
+  { type: "anthropic", apiKey: "official-secret" },
+  {},
+  new URL("https://api.anthropic.com/v1/messages"),
+).headers;
+
+assert.strictEqual(officialHeaders["x-api-key"], "official-secret");
+assert.strictEqual(
+  officialHeaders.Authorization,
+  undefined,
+  "official Anthropic keeps its x-api-key-only contract",
+);
+
+const routerSrc = fs.readFileSync(
+  path.join(__dirname, "../vendor/外接api/core/dao_router.js"),
+  "utf8",
+);
+assert.ok(
+  !routerSrc.includes("Anthropic: 移除 Authorization Bearer"),
+  "router must not strip Authorization on Anthropic-compatible relays",
+);
+
+console.log("anthropic relay auth selftest: PASS");
diff --git a/vendor/bundled-origin/source.js b/vendor/bundled-origin/source.js
index b719ab7..62f8466 100644
--- a/vendor/bundled-origin/source.js
+++ b/vendor/bundled-origin/source.js
@@ -320,7 +320,7 @@ const ORIGIN_VERSION_BASE = "v9.9.353"; // v9.9.353 · 官方直通502根治收
 //        (七十六章「人之生也柔弱 · 其死也仞贤强 · 强大居下 柔弱微细居上」)
 // 承印 152 (两经归一) · 默 canon=laozi+yinfu · 帛书老子 + 道藏阴符 (二经合 ~7670 字)
 // 承印 151 (jiqi) · webview IIFE 死活诊 + template-literal `\n` 修
-const ORIGIN_VERSION = "v9.9.421-dao-fa-zi-ran";
+const ORIGIN_VERSION = "v9.9.422-dao-fa-zi-ran";
 let _actualPort = PORT; // listening / start.onListen 时更新为 server.address().port
 const UPSTREAM_MGMT = "server.self-serve.windsurf.com";
 const UPSTREAM_INFER = "inference.codeium.com";
diff --git "a/vendor/\345\244\226\346\216\245api/core/adapters.js" "b/vendor/\345\244\226\346\216\245api/core/adapters.js"
index 2b56a60..fc796f2 100644
--- "a/vendor/\345\244\226\346\216\245api/core/adapters.js"
+++ "b/vendor/\345\244\226\346\216\245api/core/adapters.js"
@@ -489,6 +489,9 @@ const AnthropicAdapter = {
     // 认证
     if (provCfg.apiKey) {
       headers["x-api-key"] = provCfg.apiKey;
+      if (String(provCfg.type || "").toLowerCase() !== "anthropic") {
+        headers.Authorization = `Bearer ${provCfg.apiKey}`;
+      }
     } else if (provCfg.authHeader) {
       const [k, v] = provCfg.authHeader.split(":");
       if (k && v) headers[k.trim()] = v.trim();
diff --git "a/vendor/\345\244\226\346\216\245api/core/dao_router.js" "b/vendor/\345\244\226\346\216\245api/core/dao_router.js"
index 1ab8494..c47a432 100644
--- "a/vendor/\345\244\226\346\216\245api/core/dao_router.js"
+++ "b/vendor/\345\244\226\346\216\245api/core/dao_router.js"
@@ -4667,11 +4667,6 @@ async function _callProvider(
       );
       extraHeaders = { ...extraHeaders, ...(adapterOpts.headers || {}) };
 
-      // ★ Anthropic: 移除 Authorization Bearer (使用 x-api-key)
-      if (_protocol === "anthropic" && extraHeaders["Authorization"]) {
-        delete extraHeaders["Authorization"];
-      }
-
       const body = JSON.stringify(adapterBody);
       const isHttps = targetUrl.protocol === "https:";
       const mod = isHttps ? https : http;
